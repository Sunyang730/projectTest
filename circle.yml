## Tests
test:
  pre:
    - grunt build
  override:
    - grunt test

# ## Deployment options
# deployment:
#   default:
#     branch: [master, stage, dev]
#     commands:
#       - grunt clean coffee package publish:"$CIRCLE_BRANCH"