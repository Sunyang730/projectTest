## Tests
test:
  override:
    - grunt build
  post:
    - grunt test

# ## Deployment options
# deployment:
#   default:
#     branch: [master, stage, dev]
#     commands:
#       - grunt clean coffee package publish:"$CIRCLE_BRANCH"